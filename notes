onChange={(e) => {
              formik.setFieldValue(
                "shareValue",
                formik.values.projectValue / formik.values.totalShares
              );
              formik.handleChange(e);
            }}